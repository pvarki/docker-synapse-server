[project]
name = "synapse-dist"
version = "1.0.0+260428"
description = "Helper package to lock deps for building Synapse container"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "authlib>=1.6.9",
    "matrix-synapse>=1.141.0",
    "psycopg2-binary>=2.9.11",
]
